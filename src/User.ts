//when we create and export a class we give it a capital name

import faker from 'faker';

class User{
    name: string;
    location: {
        lat: number;
        lng: number;
    }

    constructor(){
        
    }
}